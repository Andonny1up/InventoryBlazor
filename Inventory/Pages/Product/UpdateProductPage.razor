@page "/product/update/{idProduct}"

<UpdateProductComponen idProduct= "@idProduct"/>

@code {
    [Parameter]
    public string idProduct { get; set; }

    protected override async Task OnInitializedAsync()
    {
        idProduct = idProduct;
    }
}